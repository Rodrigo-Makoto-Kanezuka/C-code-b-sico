#include<stdio.h>
int main () {
	int a = 2; //Indicando e passando vaalores para minha variável inteira
	while (a < 20) { //Enquanto a menor que 20, segue
		if (a % 2 == 0){ //Se a for par, segue
		printf ("%i\n", a);} //exibe o a.
		a++;} //Sempre acrementa 1 ao a...
	return 0;} //fim...
